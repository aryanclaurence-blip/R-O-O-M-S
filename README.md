# Room Dimension Manager

Pure pyRevit extension for calculating room length and width from Revit room boundaries.

## Installation

Add `D:\ANTIGRAVITY\ROOMS` as a pyRevit extension search path and reload pyRevit.

The extension exposes one command only:

`ROOMS` → `L × W` → `Room Dimension Manager`
